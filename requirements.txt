# RigMo-VAE training dependencies (pinned to a known-good set).
#
# PyTorch: install a build matching your CUDA version first, e.g.
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu124
torch==2.5.1
torchvision==0.20.1

pytorch-lightning==2.2.4
numpy==1.26.4
einops==0.8.0
omegaconf==2.3.0
jaxtyping==0.2.28
typeguard==2.13.3
packaging

# diffusers provides ModelMixin used by the base module (pulls transformers stack)
diffusers==0.32.2
transformers==4.48.0
huggingface-hub==0.26.2
safetensors==0.4.3

# data / mesh IO
trimesh==4.3.2

# logging + visualization
tensorboard==2.16.2
tensorboardX==2.6.2.2
wandb==0.18.6
matplotlib==3.8.4
plotly==6.6.0
imageio==2.34.1
opencv-python-headless==4.10.0.84
pillow==10.3.0
